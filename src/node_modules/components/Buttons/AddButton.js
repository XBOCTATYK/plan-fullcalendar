import React from 'react';
import Button from '@material-ui/core/Button';
import './Buttons.css';

const AddButton = (props) => {
    
    return (

            <Button variant="outlined" onClick={props.onClick} {...props}>
                {props.children}
            </Button>

    );
};

export default AddButton;
