import React from 'react';
import AddButton from "components/Buttons/AddButton";
import EditIcon from '@material-ui/icons/Edit';
import DeleteIcon from '@material-ui/icons/Delete';
import RedoIcon from '@material-ui/icons/Redo';
import AccessTimeIcon from '@material-ui/icons/AccessTime';

export const EditButton = ({onClick, label}) => {
    return (
        <AddButton title={label} aria-label={label} onClick={onClick}>
            <EditIcon/>
        </AddButton>
    );
};

export const DeleteButton = ({onClick, label}) => {
    return (
        <AddButton title={label} aria-label={label} onClick={onClick}>
            <DeleteIcon/>
        </AddButton>
    );
};

export const CheckTimeButton = ({onClick, label}) => {
    return (
        <AddButton title={label} aria-label={label} onClick={onClick}>
            <AccessTimeIcon/>
        </AddButton>
    );
};

export const ToTaskButton = ({taskId, label}) => {
    return (
        <a rel="noopener noreferrer" target="_blank" href={`https://cp.webpractik.ru/workgroups/group/413/tasks/task/view/${taskId}/`}>
            <AddButton title={label} aria-label={label}>
                <RedoIcon/>
            </AddButton>
        </a>
    );
};
