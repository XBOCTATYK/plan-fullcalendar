import React from 'react';
import Zone from "components/DaysList/Zone";
import {observer} from "mobx-react";
import "./DaysList.css";

const DaysList = observer((props) =>
     (
        <div className={'days-list'}>
            <div className={'days-list__header'}>

            </div>
            <div className={'days-list__body'}>
                <Zone startDate={props.startDate}
                      zoneId={'productive'}
                      name={'Продуктивное время'}
                      planItems={props.planItems}
                      planItemsArr={props.planItemsIds}
                      daysCount={props.daysCount}
                />
            </div>
        </div>
    )
);

export default DaysList;