import { decorate, observable, action } from "mobx";

export class controlPanelEntity {

    user1 = localStorage.getItem('user1');
    user2 = localStorage.getItem('user2');
    type = 0;
    days = 5;
    splitCalendar = false;

    constructor(initialParams) {
        if (initialParams) {
            this.user1 = initialParams.user1;
            this.user2 = initialParams.user2;
            this.type = initialParams.type;
            this.days = initialParams.days;
            this.splitCalendar = initialParams.splitCalendar;
        }
    }

    toggleSplit() {
        this.splitCalendar = !this.splitCalendar
    }
}

decorate(controlPanelEntity, {
    user1: observable,
    user2: observable,
    type: observable,
    days: observable,
    splitCalendar: observable,
    toggleSplit: action
});
