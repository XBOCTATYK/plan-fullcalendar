

export const setUser = function (entity, event) {
  entity.user = event.target.value;
};

export const setDays = function (entity, event) {
  entity.days = event.target.value;
};

export const setType =function (entity, event) {
    entity.plannedSwitch = event.target.value;
};