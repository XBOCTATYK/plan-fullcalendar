export default class ByGlobalObject {
    constructor(name) {
        if (!name) throw new Error('GetData-GlobalObject: Не передано имя глобальной переменой');
        this.object = window[name];
    }

    get() {
        return new Promise((resolve, reject) => {
            resolve(this.object);
        })
    };

}