import $ from 'jquery';

export default class EventModel {
    async getList() {
        return new Promise( (resolve, reject) => {
            $.get('/mockRequests/getProjects.json').done((result) => {
                resolve(result);
            })
                .fail((err) => {
                    reject(err);
                })
        })
    }

    async getOne(id) {
        return new Promise( (resolve, reject) => {
            $.get('/mockRequests/getOneProject.json', {id: id}).done((result) => {
                resolve(result);
            })
                .fail((err) => {
                    reject(err);
                })
        })
    }
}
