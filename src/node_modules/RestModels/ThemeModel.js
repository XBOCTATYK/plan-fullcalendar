import $ from 'jquery';

export default class EventModel {
    async getAll(themeName) {
        return new Promise( (resolve, reject) => {
            $.get('/appParams/themeParams.json').done((result) => {
                resolve(result[themeName]);
            })
                .fail((err) => {
                    reject(err);
                })
        })
    }
}
